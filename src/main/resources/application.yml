spring:
  application:
    name: oauth2-resourceserver-1-20250105
  datasource:
    url: jdbc:postgresql://localhost:5432/oauth2_idea
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081

logging:
  level:
    org.springframework.security: trace
